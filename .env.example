# Finnhub API key (free tier works). Enables the real XAU/USD broker feed
# (OANDA / IC Markets) as the top-priority price source. Without it the game
# uses PAXG/USD from public exchange sockets.
VITE_FINNHUB_TOKEN=

# Optional: server-side webhook that receives every email lead as JSON.
LEAD_WEBHOOK_URL=
