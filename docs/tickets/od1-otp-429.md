TICKET OD1: owner-reported defect - gibberish OTP codes lead to a 429, and a later email "got in" with rewards.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Open defects OD1, S2, S13, B9, C3), server/index.js (handleAuth, request_otp, verify_otp, the HTTP upgrade path and the 429 refusals), server/limits.js, server/otp.js, db/schema.sql (request_otp_code, verify_otp_code, the 5-attempt rule, release_task_reward on verify), src/Identity.jsx, src/api/socket.js (reconnect behaviour after an error frame), src/useGame.js, tests/e2e/web-identity.spec.js, test/integration-box/otp.test.mjs, test/integration-box/limits.test.mjs. Never open a file named .env. Never commit. Work only in this worktree.

What the owner saw on the local compose stack (one browser, one IP): entered `t1w@f.com`, got the OTP dialog, typed several gibberish codes, and the browser console showed a **429**. A few attempts later, with different emails, the owner "got in" and was credited rewards on that account.

Part 1, reproduce as the owner did, with an E2E spec (tests/e2e/od1-otp.spec.js) driving the real UI: request a code for one email, type 6 wrong codes, capture every console error and every socket close code, then request codes for three more emails in a row. Record exactly which limit produced the 429 (HTTP upgrade refusal from S2's per-IP connection or socket window, the per-IP OTP window of 5 per 10 minutes, or the per-email window of 3) and what the client did after `too_many_attempts` (does src/api/socket.js reconnect, and does each reconnect burn the connection window?). Paste the sequence in the report.
Part 2, decide and fix, within these rules: a single visitor typing wrong codes must never see a 429; the 5-attempts-per-code rule (S13) stays and must show the existing "too many attempts, request a new code" copy; the per-IP OTP windows (S2) stay but are raised to venue-safe values: 30 requests per 10 minutes per IP and 5 per email; a `too_many_attempts` or `invalid_code` error must not close or reconnect the socket; a 429 on the upgrade path must show the existing reconnect overlay with a "Too many connections from this network, try again in a minute" line (fa + en) instead of a silent console error.
Part 3, the "got in with rewards" question: prove with an integration test whether a player can end up verified without a correct code for that email (check verify_otp_code's lookup by email and the code's `used_at`/expiry), and whether the email/signup reward (B9) can be released without a completed verify. If either is possible, that is a security defect: fix it in SQL and say so first in the report. If neither is possible, show the passing tests and explain how the owner's later emails were verified (the local stack logs the OTP code to the server log and the dev peek endpoint; name the path).
Tests: the E2E from part 1 turned into the regression spec (no 429, correct copy after 5 wrong codes, a fresh email still works); integration for part 3; unit for any limits change. Dev DB: copy db/run-tests.sh with PORT=55462 and container goldrush-od1-keep; server on PORT=8803 with VITE_GAME_WS=ws://localhost:8803/ws on the Vite command line; Vite on 5364. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, pgTAP, test:server, build, the full E2E suite. Report: bad news first (any way to verify without a code), the exact 429 source, judgement calls, files touched, gate outputs.
