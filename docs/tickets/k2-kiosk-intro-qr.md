TICKET K2: the kiosk intro becomes an exciting three-card experience whose first showing ends on a QR code for the web version; the kiosk has no coins or missions section.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration, C11, C9, C2), src/KioskApp.jsx (KioskIntroModal), src/useKioskFlow.js (showIntro, dismissIntro, introShownThisBoot, streakTarget), src/i18n.js (kioskIntro), src/styles.css (kiosk modal classes, the QR classes from C9), the qrcode dependency use in src/ (C9's QR rendering), src/App.jsx (nav tabs, `!IS_KIOSK` gates), tests/e2e/c11-tours.spec.js, tests/e2e/kiosk.spec.js, tests/e2e/first-visit.js. Never open a file named .env. Never commit. Work only in this worktree. Design fidelity: existing tokens and typography; you may add CSS keyframes for motion, no new colours or libraries.

Decisions (do not redesign; the visual treatment inside these bounds is yours):
1. Three cards, kiosk only, both languages on every card (fa above, en below, like the C9 QR screen), large type readable from a metre away, one primary button each, dots for progress, a subtle looping animation per card (gold coin flip, rising chart line, confetti burst; CSS only, respects `prefers-reduced-motion`):
   - Card 1 "Call gold for 5 seconds": up or down, tap and the round runs.
   - Card 2 "Win {n} in a row": the $100 bonus, with the streak target from `kiosk_session.streak_target` (fallback 5 before the first frame).
   - Card 3 "Take Gold Rush with you": a QR code (same qrcode rendering as C9) of `PUBLIC_URL` (the web version), with "Scan to play on your phone and climb the leaderboard" and the Start button.
2. Once per boot, as today (`introShownThisBoot`); the QR card belongs to that first showing. Any card's button counts as activity for the idle timer (dismissIntro already does). The shared E2E helper (tests/e2e/first-visit.js) must keep working: the terminal button is still named "Start"; intermediate buttons "Next".
3. Kiosk chrome: the kiosk renders no coins, missions, leaderboard, profile or lead-capture UI anywhere (C2's rule); verify the nav and the balance chip: the balance chip stays (coins are the game), the tab bar does not exist on the kiosk. If anything of the web nav leaks onto the kiosk today, remove it and extend the existing `forbiddenUi` assertion in tests/e2e/kiosk.spec.js.
4. `PUBLIC_URL` reaches the client through the `kiosk_session` frame as `public_url` (server already knows it, C9); the QR card is skipped, not broken, when it is empty.
Tests: E2E kiosk (three cards in order, both languages present, QR card renders an `<img>` or `<svg>` whose data decodes to `PUBLIC_URL` - use the qrcode package's decode-free check: assert the alt text or a `data-url` attribute carries the URL; Start goes to play; second tap in the same boot shows no intro; forbidden UI absent). Screenshots of each card to docs/reports/k2/. Dev DB: copy db/run-tests.sh with PORT=55468 and container goldrush-k2-keep; server on PORT=8809 with PUBLIC_URL=http://localhost:5371 and VITE_GAME_WS=ws://localhost:8809/ws on the command line; Vite on 5371. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, build, the FULL E2E suite. Report: bad news first, judgement calls, files touched, gate outputs.
