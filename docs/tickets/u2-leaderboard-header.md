TICKET U2: leaderboard header fused with the tournament card; the guest note takes the own-row slot.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration, B1, B2), src/App.jsx (Leaderboard, TournamentHeader, the `.lb-row-me` and `.lb-row-sticky` paths), src/styles.css (`.lb-*`, `.lb-tournament-*`), src/i18n.js (`lb.guestNote`, `tournament.*`), tests/e2e/leaderboard-paging.spec.js, tests/e2e/tournament.spec.js, tests/e2e/web-identity.spec.js. Never open a file named .env. Never commit. Work only in this worktree. Design fidelity: existing tokens and classes only.

Decisions (do not redesign):
1. One header card. Today the screen shows a title block ("🏆 Leaderboard / by record") and, below it, a tournament card with a prize image, title, dates and prize title, then the tournament switcher. Fuse the first two into one card: the cup icon that is already next to the "Leaderboard" title moves to the left of the card (48 px, replacing the prize image entirely; `prize_image` is no longer rendered anywhere), the card's first line is "Leaderboard" with "by record" under it in the existing small style, and the tournament title, date range with time left, and prize title follow on the same card in their existing styles. When no tournament is running, the card shows "Leaderboard / by record" and the existing "No tournament running" line inside the same card.
2. The switcher stays under the card and does what it does now: choosing a past or upcoming tournament changes which tournament the card describes and which board is listed. Its buttons keep the existing tab classes.
3. Guest note. The green "Play as guest - add your email to be ranked" row is unreadable: use the existing `.lb-row-me` styling with the text in the normal row text colour (no green-on-green), the cup/rank cell empty, and keep it a button that opens the identity screen. Placement is the own-row rule from B2 applied to a logged-out player: it renders where that player's own row would be, which means below the last listed row when the list fits in its container, and pinned to the bottom of the list container (the existing `.lb-row-sticky` treatment) when the list overflows. A verified player never sees it.
4. No new colours, fonts or spacing systems. Screenshots before and after, web only, to docs/reports/u2/: empty board as guest, full board as guest (overflowing), verified player, past tournament selected.
Tests: update the existing leaderboard E2E specs for the fused card (the tournament title and dates are inside `.lb-head` now; `.lb-tournament-prize` no longer exists); E2E for the guest row placement in both the fits and overflows cases (seed 25 scored players through the DB helper the paging spec already uses). Dev DB: copy db/run-tests.sh with PORT=55464 and container goldrush-u2-keep; server on PORT=8805 with VITE_GAME_WS=ws://localhost:8805/ws on the Vite command line; Vite on 5367. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, build, the FULL E2E suite. Report: bad news first, judgement calls, files touched, gate outputs.
