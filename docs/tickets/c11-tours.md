TICKET C11: real first-visit tour (web) and kiosk intro on the B10 mount points.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (C11, B10, A5, A6), docs/tasks-marketing-lead.md (A5, A6), src/App.jsx (TourPlaceholder and where it mounts), src/KioskApp.jsx (KioskIntroModal), src/useGame.js (tourSeen, markTourSeen), src/useKioskFlow.js (showIntro, dismissIntro), src/i18n.js, src/styles.css, tests/e2e/first-visit.js, tests/e2e/b10-b11-tour-ads.spec.js. Never open a file named .env. Never commit. Work only in this worktree.

Decisions (do not redesign):
1. Web tour: replace the placeholder body with three cards inside the same `.modal-backdrop .modal` shell, one at a time, with dots and a Next button; the last card's button is "Got it" (keep that label: tests/e2e/first-visit.js clicks it). Cards, each a title and one sentence, in `fa` and `en` in src/i18n.js: (1) "Call the next 5 seconds" / "Gold goes up or down. Pick one before the round starts." (2) "Win coins, climb the board" / "Every right call adds to your record; the top of the board wins the prize." (3) "Verify your email to be ranked" / "Anonymous play is fine; only verified emails appear on the leaderboard." A Skip link on cards 1 and 2 also ends the tour. `markTourSeen()` fires on Got it and on Skip.
2. Kiosk intro: one card, larger type for the booth, in both languages at once (like the C9 QR screen): "Predict gold for 5 seconds. Win 5 in a row and take home the $100 bonus." with one button "Start". The button keeps the accessible name "Got it" for the shared test helper (visible label "Start" via aria-label "Got it" is not acceptable; instead update tests/e2e/first-visit.js to accept either "Got it" or "Start"). Counts as activity for the idle timer (already true through dismissIntro).
3. Design fidelity: existing modal classes, colour tokens and typography only; no new colours, fonts or libraries. Screenshots of every card, web and kiosk, to docs/reports/c11/.
4. The streak target in the kiosk copy is not hard-coded: read `streak_target` from the kiosk_session frame (C9) and fall back to 5 before the first frame.
Tests: E2E web (three cards, Next, Skip on card 1 ends the tour, no tour after reload); E2E kiosk (intro shows once per boot, Start goes to play, the existing kiosk suite still passes). Dev DB: copy db/run-tests.sh with PORT=55460 and container goldrush-c11-keep; server on PORT=8801 with VITE_GAME_WS=ws://localhost:8801/ws on the Vite command line; Vite on 5362. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, build, the FULL E2E suite. Report: bad news first, judgement calls, files touched, gate outputs.
