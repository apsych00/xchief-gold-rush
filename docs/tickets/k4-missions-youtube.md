TICKET K4: the Coins tab becomes Missions; YouTube videos with a watch-progress player that releases the reward when a video is really watched; redirect rewards release the moment their timer ends.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration, B6, B7, B12), src/Tasks.jsx (the video modal, the redirect flow, the task rows), src/useGame.js (reportTaskProgress, startTaskVisit, returnTaskVisit), src/config.js, src/i18n.js (nav labels, tasks copy), server/index.js (task_progress, task_start, task_return), db/schema.sql (tasks with kind and url, video_progress, report_video_progress, task_visits, return_task_visit), db/seed.sql, test/integration-box/tasks.test.mjs, tests/e2e/player-promises.spec.js (tests 6 and 7). Never open a file named .env. Never commit. Work only in this worktree. Design fidelity: existing tokens and classes; the YouTube IFrame Player API is the one allowed external script (loaded only on the Missions screen, web only).

Decisions (do not redesign):
1. Rename: the tab and screen title become "Missions" (fa: "ماموریت‌ها"); every i18n key that said Coins for the tab changes; the balance chip keeps the word coins.
2. YouTube missions: the seed gains three `kind='youtube'` tasks with `url` = the video id (placeholders `dQw4w9WgXcQ`-style are NOT acceptable: use the three most recent public videos under one minute from the xChief YouTube channel if you can find their ids with a web fetch of the channel page; if not, use `VIDEO_ID_1..3` placeholders and say so first in the report), each 150 coins, once per device or email. The row opens the same modal as the promo video, with the YouTube IFrame Player: `controls: 0`, `disablekb: 1`, `modestbranding: 1`, `rel: 0`, `playsinline: 1`, no seek bar; the player is not seekable through the UI. Progress: every second the client reads `getCurrentTime()` and reports `task_progress {task, seconds, duration}` every 5 s and on `ENDED`; `seconds` is the accumulated watched time counted from play-state deltas capped at 1.5 s per tick, so a programmatic seek does not count time it skipped (a scripted tweak is acceptable, per the owner). The server side (`report_video_progress`) already releases at 90 % and refuses jumps; add the `youtube` kind to its accepted kinds.
3. Redirect timer: today the row shows a countdown and then sits at zero until the next focus event. Fix: when the 5 s window ends the client sends `task_return` immediately (a timer in the row, not only on focus) and the row moves to the claimed state on the server's `me` frame; if the server answers `not_yet` (clock skew) retry once after `retry_ms`. The same for every other "fake kind" redirect task the owner mentioned (Telegram, YouTube subscribe, reviews, story share): one code path.
4. Video modal for the promo video keeps working; a mission that is already claimed shows the claimed state and opens nothing.
Tests: unit for the watched-time accumulator (a 30 s jump adds at most 1.5 s); integration for the youtube kind on `report_video_progress`; E2E (Missions tab title; a redirect mission releases within 1 s after the timer ends without any focus event, driven with the existing fake window; the YouTube modal renders the player and reports progress - mock the IFrame API in the test with a stub `window.YT` so no network is needed). Screenshots to docs/reports/k4/. Dev DB: copy db/run-tests.sh with PORT=55470 and container goldrush-k4-keep; server on PORT=8811 with VITE_GAME_WS=ws://localhost:8811/ws on the Vite command line; Vite on 5373. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, pgTAP, test:server, build, the FULL E2E suite. Report: bad news first, judgement calls, files touched, gate outputs.
