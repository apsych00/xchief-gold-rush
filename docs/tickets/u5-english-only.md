TICKET U5: English only. Every page, modal, card, toast, kiosk screen and email the app renders carries English copy only; the Persian locale is removed from the codebase, wiped clean. The second locale returns in a later version with a proper language switcher, so leave the i18n plumbing in place but with one language.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration, U5), src/i18n.js (dict.fa, dict.en, LANGS, DEFAULT_LANG, num, makeT, LangContext, the "read directly by key" bilingual entries: qrTitleFa/qrTitleEn, scannedBtnFa/scannedBtnEn, kioskIntro.fa/en), every consumer of LangContext, setLang or lang (src/App.jsx, src/KioskApp.jsx, src/Identity.jsx, src/Profile.jsx, src/Tasks.jsx, src/ClaimPage.jsx, src/LeadCapture.jsx, src/SignupForm.jsx, src/UpdateBanner.jsx, src/af/Landing.jsx), src/styles.css and src/*.css (every `[dir="rtl"]`, `.rtl`, `:lang(fa)` rule and every Persian font-face), index.html (lang/dir attributes, font preloads), tests/e2e/c11-tours.spec.js, tests/e2e/kiosk.spec.js, tests/e2e/smoke.spec.js, tests/e2e/web-identity.spec.js, tests/e2e/first-visit.js, test/unit (any i18n test). Never open a file named .env. Never commit. Work only in this worktree. Design fidelity: no visual change other than the Persian text and the language toggle disappearing; layouts that stacked fa above en (the C9 QR screen, the C11 kiosk intro) keep the English block only, re-centred.

Decisions (do not redesign):
1. `src/i18n.js`: delete `dict.fa` entirely; `LANGS = ['en']`, `DEFAULT_LANG = 'en'`; `num()` returns Western digits only; `makeT`, `t`, `LangContext` and the `useLang`-style hook keep their signatures so components do not change shape; `setLang` becomes a no-op and the `xchief.lang` localStorage key is neither read nor written (remove the read; an old stored value must not switch anything). Bilingual "read directly by key" entries collapse to one English string each (qrTitle, scannedBtn, kioskIntro).
2. Every language toggle control (header, kiosk, claim page, landing) is removed from the JSX, not hidden. Every `dir` attribute toggle goes; the document is `lang="en" dir="ltr"`. Every rtl-only CSS rule and Persian font-face/preload is removed. Keep a short list of what was removed in the report so the next version can bring the switcher back deliberately.
3. Server-side strings that reach the user (OTP email subject and body in server/, Telegram alert copy, error copy in `server/index.js`) are English only too; audit and fix any Persian there.
4. Docs are out of scope (docs/*.fa.md stay).
Tests: unit: `LANGS` equals `['en']` and `makeT('fa')` falls back to English; E2E: a new `tests/e2e/english-only.spec.js` visits the web home, opens the tour, the leaderboard, the missions/coins tab, the profile, the share modal if present, the claim page (`/claim/nope` is fine), the kiosk attract screen and its intro, and asserts the rendered `document.body.innerText` contains no character in U+0600..U+06FF on any of them; the four existing specs that matched Persian strings are updated to the English shape. Dev DB: copy db/run-tests.sh with PORT=55481 and container goldrush-u5-keep; server on PORT=8814 with VITE_GAME_WS=ws://localhost:8814/ws on the Vite command line; Vite on 5376. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, build, test:server, the FULL E2E suite. Report: bad news first, the removed-list from decision 2, files touched, gate outputs.
