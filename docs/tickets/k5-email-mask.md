TICKET K5: a nicer email mask that shows more of a long address while keeping it private, applied on the server everywhere an email leaves it.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration, C4, S16), db/schema.sql (mask_email and every caller: leaderboard, my_rank, get_me, claim_prize's `email_masked`, share endpoints), server/index.js and server/ledger.js (every place an email is put on the wire: welcome/me, leaderboard push, /api/claim GET, /api/share, /status, alerts), src/ (any client-side masking or raw email display: Identity.jsx header, leaderboard rows, claim page, profile), db/tests/20_leaderboard_view.sql and the mask tests, test/integration-box/leaderboard.test.mjs, tests/e2e/web-identity.spec.js. Never open a file named .env. Never commit. Work only in this worktree.

Decisions (do not redesign):
1. New rule for `mask_email(text)`, in SQL, immutable: split at the last `@`; local part `L` of length n: n <= 2 -> first char + `***`; 3..5 -> first char + `***` + last char; 6..9 -> first 2 + `****` + last 2; n >= 10 -> first 3 + `*****` + last 3. Domain: shown in full when it is one of the twenty most common consumer domains (gmail.com, yahoo.com, outlook.com, hotmail.com, icloud.com, proton.me, protonmail.com, live.com, msn.com, aol.com, mail.com, yandex.com, yandex.ru, mail.ru, gmx.com, zoho.com, me.com, ymail.com, googlemail.com, hey.com); otherwise the domain's first label is masked to its first char + `**` and the rest kept (`j**.example.com` style), so a company address does not identify the employer. Examples in the test: `pedram.adsency@gmail.com` -> `ped*****ncy@gmail.com`; `ab@x.io` -> `a***@x**.io`; `farid@acme-corp.co` -> `f***d@a**.co` is wrong: it must be `f***d@a**-corp.co`? No: the rule masks the first label to first char + `**` and keeps the rest of the label's suffix beyond the TLD unchanged, i.e. `acme-corp.co` -> `a**.co`. Write the test cases from these rules, not from this example, and paste the resulting table in the report.
2. The server is the only place that masks; the client never sees a raw email except the player's own on the identity screen (get_me returns `email` for the owner and `display` for everyone else, as today). Audit every wire path listed above and remove any client-side masking helper that still exists; the leaderboard, share page, claim page and Telegram alerts carry masked values only. Add an integration test that opens two sockets (A verified, B anonymous) and asserts that nothing B receives contains A's raw email, and that the alerts transport (fake fetch) never receives a raw email.
3. Own-row identity stays by player id (B2); the E2E assertions that match masked strings (web-identity, leaderboard-paging, tournament) are updated to the new shape.
Tests: pgTAP table for the rule (at least 10 cases incl. unicode local parts and a subdomain domain); integration as in 2; E2E updates. Dev DB: copy db/run-tests.sh with PORT=55471 and container goldrush-k5-keep; server on PORT=8812 with VITE_GAME_WS=ws://localhost:8812/ws on the Vite command line; Vite on 5374. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, pgTAP, test:server, build, the FULL E2E suite. Report: bad news first, the mask table, judgement calls, files touched, gate outputs.
