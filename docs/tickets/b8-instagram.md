TICKET B8: Instagram follow reward, verified through the Instagram Graph API, built now and switched on the day the credentials arrive. Depends on B6+B7+B9 being merged into dev; branch from dev after that merge.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (B8), docs/tasks-marketing-lead.md (A7, the Instagram lines), docs/tickets/b6-b7-b9-rewards.md (task kinds and the server-released reward shape), db/schema.sql (tasks, task_claims, devices), server/index.js, src/Tasks.jsx, .env.box.example, docs/box-deploy.md. Never open a file named .env. Never commit. Work only in this worktree. No live Instagram call is possible here; everything is built against the documented API and proven with a fake.

Decisions (do not redesign):
1. What Instagram allows: no third-party endpoint answers "does user X follow account Y". So verification means: the player completes Instagram Login (OAuth) with our app, which proves a real Instagram account and gives us its `user_id` and `username`; the reward is bound to that account once, and to the device and verified email per B5. After OAuth the client deep-links to our profile (`INSTAGRAM_PROFILE_URL`) with a "Follow, then come back" step; the return is not verified (same leniency as B7, hardened in B13). Adapter `server/instagram.js` exports `authorizeUrl({state})`, `exchangeCode({code})` (POST `https://api.instagram.com/oauth/access_token`) and `readMe({token})` (GET `https://graph.instagram.com/me?fields=id,username`), each returning `{ok:true, ...}` or `{ok:false, code}`. Table `public.instagram_accounts(ig_user_id text primary key, username text, player_id uuid, device_id uuid, verified_at)`; a second player or device presenting the same `ig_user_id` is refused with `already_claimed`. State this limitation plainly in docs/box-deploy.md.
2. Env (`.env.box.example`, all optional, adapter reports `not_configured` when any is missing): `INSTAGRAM_APP_ID`, `INSTAGRAM_APP_SECRET`, `INSTAGRAM_REDIRECT_URI` (`https://goldrush.xchief.academy/api/instagram/callback`), `INSTAGRAM_PROFILE_URL`. HTTP: `GET /api/instagram/start?token=<player token>` redirects to the Instagram authorise URL with `state` = HMAC of the player id; `GET /api/instagram/callback` exchanges the code, stores the account, releases the `instagram` task through the same server-side path as B6, and redirects to `/?ig=done` or `/?ig=<error code>`. The client task row for kind `instagram` opens `/api/instagram/start` in the same tab and shows the pending, done and failed states from the query parameter (copy: "Connecting Instagram", "Followed! +300 coins", "Instagram did not confirm. Try again.").
3. `/status` gains `instagram: 'configured' | 'not_configured'`. Rate limit the two endpoints with S2's per-IP window.
4. A fake Instagram server for tests (`test/fakes/instagram.mjs`) implementing the authorise redirect, the token exchange and the `/me` read, selected by `INSTAGRAM_API_BASE` (default the real host).
Tests: unit (state HMAC, adapter parses success and each documented error); integration with the fake (happy path releases once; same ig_user_id from a second device refused; missing env gives not_configured and the task row says "coming soon"); E2E web with the fake (click, callback, toast). Dev DB: copy db/run-tests.sh with PORT=55458 and container goldrush-b8-keep; server on PORT=8799 with VITE_GAME_WS=ws://localhost:8799/ws on the Vite command line; Vite on 5358. Never set FINNHUB_TOKEN. Delete the copy and container when done. Screenshots to docs/reports/b8/. Gates unpiped: lint, unit, pgTAP, test:server, build, E2E. Report: bad news first (including anything in the Instagram API docs that contradicts decision 1; cite the page), judgement calls, files touched, gate outputs, and the exact "credentials arrived" checklist for docs/box-deploy.md.
