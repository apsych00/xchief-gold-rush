TICKET U1: client polish batch (owner's recalibration, 2026-09-17 evening): remove the wipe button, user icon instead of the "Y" avatar, themed scrollbars, one socket only.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration), src/App.jsx (TopBar avatar), src/Profile.jsx (pf-actions), src/useGame.js (resetProfile), src/profile.js, src/priceFeed.js, src/api/socket.js, src/styles.css, src/profile-screen.css, tests/e2e/. Never open a file named .env. Never commit. Work only in this worktree. Design fidelity: existing tokens and classes only.

Decisions (do not redesign):
1. Wipe button: remove the "reset my progress" / "Yes, wipe my progress" buttons from src/Profile.jsx. Keep `resetProfile` in src/useGame.js and src/profile.js untouched (the dev hook `window.__xchief` and tests may still call it); delete only the UI path. Remove the now-unused i18n keys only if nothing else references them.
2. Avatar: replace the initials letter in the top bar avatar button with this exact SVG, inline in JSX, stroke colour from the existing `--gold` token instead of the literal red (`stroke="currentColor"` with `color: var(--gold)` on the button, so the level classes keep working):
   `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>`
   The flame badge for a streak stays. The profile screen's large avatar (`.pf-avatar`) shows the same icon at 48 px.
3. Scrollbars: one global rule set in src/styles.css that themes every scrollable region to the app: `scrollbar-width: thin; scrollbar-color: rgba(255,255,255,0.18) transparent` on `*`, plus the WebKit equivalents (`::-webkit-scrollbar` 6 px wide, transparent track, thumb `rgba(255,255,255,0.18)` with radius 3 px, hover `rgba(255,255,255,0.3)`). No new colour tokens.
4. One socket: the client must open exactly one WebSocket, ours. Delete the direct upstream sources in src/priceFeed.js (finnhub, okx, binance, binance2, the relay URL path) and whatever remains of the client-side price fallback; the play screen shows the "connecting" state until the server's hello arrives. Remove `VITE_RELAY_URL` and `FINNHUB_TOKEN` reads from the client and from `.env.public`/`.env.production` if present there. Prove it with an E2E assertion (tests/e2e/smoke.spec.js): after a round, every WebSocket the page opened (collect them with `page.on('websocket')`) points at the game socket URL and there is exactly one.
Tests: E2E smoke assertion above; E2E that the profile screen has no wipe button and the avatar renders the icon (aria-label unchanged). Dev DB: copy db/run-tests.sh with PORT=55463 and container goldrush-u1-keep; server on PORT=8804 with VITE_GAME_WS=ws://localhost:8804/ws on the Vite command line; Vite on 5366. Never set FINNHUB_TOKEN. Delete the copy and container when done. Screenshots of the top bar and profile to docs/reports/u1/. Gates unpiped: lint, unit, build, the FULL E2E suite. Report: bad news first, judgement calls, files touched, gate outputs.
