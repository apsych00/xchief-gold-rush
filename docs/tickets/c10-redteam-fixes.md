TICKET C10: red-team fixes (D1, D7, D8, D9, plus the test-contract drift).

Read AGENTS.md, .claude/agents/builder.md, docs/reports/redteam.md (sections D1, D7, D8, D9, "Not security, but found on the way"), docs/TRACKER.md (C10, S14), db/schema.sql (start_kiosk_session, reset_kiosk_session, settle_kiosk_round, open_kiosk_round), server/index.js (handleAuth, handleFrame, the kiosk_reset and leaderboard cases), server/ledger.js (mapError, KNOWN_ERROR_CODES), server/rounds.js, src/api/socket.js (IS_KIOSK), src/api/kiosk.js, docs/test-contract.md, docs/box-spec.md, test/integration-box/server.test.mjs, db/tests/. Never open a file named .env. Never commit. Work only in this worktree. The red team's `demo/redteam.mjs` reproduces every item: run `node demo/redteam.mjs --only A16` (D1), `--only A7` (D7, D9), `--only A8` (D8) before and after; the report says how it was started (a server on its own port and DB).

Decisions (do not redesign):
1. D1 (high): `reset_kiosk_session(p_kiosk)` and `start_kiosk_session(p_kiosk)` void every round still open for that kiosk in the same statement: `update public.rounds set status='settled', outcome='void', end_at=now() where kiosk_id=p_kiosk and status='open'`. If `outcome` is a constrained enum without `void`, add it; a void round changes no coins and no streak and is excluded from every stat and export that counts wins or losses. `settle_kiosk_round` on a voided round raises `round_not_open` as today; `server/rounds.js` must log it once and drop it without touching the socket, and must never push a `round_settled` for it. The client's in-flight round on the socket that reset is cancelled by the `kiosk_session` frame the reset already returns; the kiosk screen must not show a verdict after a reset (check `src/useKioskFlow.js`, fix if it would).
2. D7: kiosk auth keys on presence, not truthiness. Server: `if (frame.kiosk !== undefined)` takes the kiosk path and an empty or short secret fails closed with `{type:'error', code:'kiosk_unauthorized'}` and the socket closed with 4401. Client: `IS_KIOSK` is true when the `k` parameter is present at all, and an empty or rejected secret renders the existing reconnect/error state (reuse the C2 reconnect overlay copy with one added line: "Kiosk not configured. Tell the booth staff."), never the web app. Nothing in kiosk mode may render email, task, leaderboard or lead-capture UI in this state; the existing "forbidden UI nodes" E2E assertion must cover it.
3. D8: the `leaderboard` request frame gets the same `kind !== 'player'` guard as its neighbours, answering `not_available`.
4. D9: `handleFrame` and `handleAuth` forward `err.code` only when it is in the exported `KNOWN_ERROR_CODES` from `server/ledger.js`; everything else becomes `internal` and the original is logged server-side with the frame type. Add every code the box-spec names to that list if it is missing; the contract in docs/box-spec.md is the source of truth.
5. Contract drift: docs/test-contract.md says 60 rounds per hour and `display_name`; the code is right (400, `display`). Edit the contract, not the code.
Tests: pgTAP (reset voids an open kiosk round; start voids too; settle on a voided round raises round_not_open; void rounds change no coins); integration (D1: play then kiosk_reset within the round, no round_settled arrives, pot stays 1000 idle; D7: `auth` with `kiosk: ''` gets kiosk_unauthorized and close 4401; D8: kiosk leaderboard request gets not_available; D9: a frame that makes Postgres raise an unmapped error yields `internal`, never a SQLSTATE); E2E kiosk: `/?k=` shows the not-configured state and none of the forbidden UI nodes. Then rerun `node demo/redteam.mjs --only A16,A7,A8` and paste the verdict lines. Dev DB: copy db/run-tests.sh with PORT=55455 and container goldrush-c10-keep; server on PORT=8796 with VITE_GAME_WS=ws://localhost:8796/ws on the Vite command line; Vite on 5355. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, pgTAP, test:server, build, E2E. Report: bad news first, judgement calls, files touched, gate outputs, the red-team verdict lines.
