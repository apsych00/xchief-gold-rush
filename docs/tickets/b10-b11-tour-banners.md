TICKET B10 + B11: tour-seen flag and the ad banner list.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (B10, B11), docs/tasks-marketing-lead.md (A1, A5, A6), server/index.js, src/api/socket.js, src/profile.js, src/useGame.js, src/useKioskFlow.js, src/App.jsx (Leaderboard), src/styles.css. Never open a file named .env. Never commit. Work only in this worktree.

Decisions (do not redesign):
B10, tour seen:
1. Web: `localStorage` key `xchief.tour_seen` = ISO timestamp, set by the client when the tour is dismissed; read on load. No server involvement (the tour is not a reward). Expose `tourSeen` and `markTourSeen()` from src/useGame.js; do not build the tour screens (A5 is the marketing lead's); mount point only: `{!tourSeen && <TourPlaceholder onDone={markTourSeen} />}` where `TourPlaceholder` is a minimal modal reusing `.modal-backdrop .modal` with the title "Welcome to Gold Rush" and one "Got it" button, to be replaced by the designed tour.
2. Kiosk: the intro shows once per boot: a module-level flag in src/useKioskFlow.js (`introShownThisBoot`) set when the kiosk intro is dismissed; the intro is a placeholder modal like the web one, shown between attract and the first play, dismissed by its single button, and it must not interfere with the idle countdown (it counts as activity when dismissed). Kiosk never touches localStorage.
B11, banners:
3. `ads/banners.json` in the repo (served by Caddy as a static file next to dist; add the mount and a `handle_path /ads*` in the Caddyfile) listing `{src, alt, href}` items; the assets go under `public/ads/`. Two placeholder PNGs (solid brand colours from the existing tokens, 1080x432) are added so the zone renders now.
4. Client: `src/ads.js` fetches `/ads/banners.json` once per load, picks a random item, rotates every 8 s with a 400 ms cross-fade (reuse the existing fade keyframe), and exposes a `<AdZone />` component. Mount it as the bottom 40 % of the Leaderboard screen (a flex column with the list above scrolling independently). Never shown on the kiosk. If the list is empty or the fetch fails, the zone renders nothing and the list takes the full height.
Tests: unit for the rotation picker (random pick, no immediate repeat, empty list); E2E: the leaderboard shows the ad zone with an image on the web, and the kiosk never renders it; the web tour placeholder appears once and not after reload; kiosk intro appears once per load of the page in a fresh context. Dev DB: copy db/run-tests.sh with PORT=55452 and container goldrush-b10-keep; server on PORT=8793 with VITE_GAME_WS on the Vite command line; Vite on 5352. Never set FINNHUB_TOKEN. Delete the copy and container when done. Screenshots to docs/reports/b10-b11/. Gates unpiped: lint, unit, build, E2E. Report: bad news first, judgement calls, files touched, gate outputs.
