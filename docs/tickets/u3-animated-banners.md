TICKET U3: the ad zone shows the two animated xChief banners and shrinks to their aspect ratio.

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration, B11), ads/banners/README.md (the integration notes that came with the banners), ads/banners.json, src/ads.jsx, src/adsPicker.js, src/styles.css (`.ad-zone*`), Caddyfile (`handle_path /ads*`), docker-compose.yml (the ads mount), vite.config.js, test/unit/ads-picker.test.mjs, tests/e2e/b10-b11-tour-ads.spec.js. Never open a file named .env. Never commit. Work only in this worktree.

Decisions (do not redesign):
1. The two files in ads/banners/ (`xchief-board-banner.html`, 20.5 s loop; `xchief-banner-concepts.html`, 11.5 s loop per concept) are the banners. They are self-contained HTML and are embedded as `<iframe src="/ads/banners/<file>?embed=1" ...>` exactly as their README says (concepts file with `?concept=N&embed=1`). They are served from the ads mount like the images today (confirm `/ads/banners/*.html` is reachable through Caddy in the compose build, and through the Vite dev server).
2. `ads/banners.json` entries gain a `type` (`image` | `iframe`) and, for iframes, `duration_ms`. Seed two iframe entries: the board banner (20500) and concept 3 of the concepts file (11500). Image entries keep working as before.
3. Rotation: an iframe banner shows for its `duration_ms` (one full loop), then the next entry, in list order, looping; the existing 400 ms cross-fade stays. No random pick for iframe entries (the order is the story); image-only lists keep the random pick the unit test covers.
4. Size: the zone's height follows the banner aspect ratio 1072:310 (`aspect-ratio: 1072 / 310`, width 100 %) instead of the fixed 40 % of the screen; the leaderboard list above reclaims the rest. The iframe has `border: 0`, `display: block`, `loading="lazy"`, `title="xChief"`, a black background, and is never focusable by keyboard (tabindex -1); clicks on it are the banner's own.
5. Never on the kiosk (unchanged).
Tests: unit for the duration-based rotation order; E2E: the zone shows an iframe whose src starts with `/ads/banners/` on the web, the zone height matches the aspect ratio within 2 px, and the kiosk never renders it. Screenshots to docs/reports/u3/. Dev DB: copy db/run-tests.sh with PORT=55465 and container goldrush-u3-keep; server on PORT=8806 with VITE_GAME_WS=ws://localhost:8806/ws on the Vite command line; Vite on 5368. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates unpiped: lint, unit, build, the FULL E2E suite. Report: bad news first, judgement calls, files touched, gate outputs.
