TICKET D5: screenshots of every screen of the app, as separate PNGs and as one high-quality sprite sheet, for handing to a design agent. Output is git-ignored (`design/screens/`).

Read AGENTS.md, .claude/agents/builder.md, docs/TRACKER.md (Recalibration), tests/e2e/first-visit.js, tests/e2e/smoke.spec.js, tests/e2e/kiosk.spec.js, tests/e2e/c11-tours.spec.js, tests/e2e/leaderboard-paging.spec.js, tests/e2e/player-promises.spec.js, tests/e2e/c9-qr-claim.spec.js (how each screen is reached and how the fake window/kiosk secret work), src/App.jsx, src/KioskApp.jsx. Never open a file named .env. Never commit. Work only in this worktree. No new dependencies: Playwright is already installed; compose the sprite by rendering an HTML contact sheet with Playwright at deviceScaleFactor 2 and screenshotting it (no sharp, no canvas package).

Decisions:
1. Script: `scripts/screens.mjs` (node, uses @playwright/test's chromium), run as `node scripts/screens.mjs` with `BASE_URL`, `KIOSK_SECRET` (dev-kiosk-secret-0001) and `VITE_GAME_WS` from the environment. Viewports: web at 390x844 (phone, deviceScaleFactor 3) and 1280x800 (desktop, factor 2); kiosk at 1080x1920 (factor 1) or the kiosk viewport the kiosk spec uses.
2. Screens, each saved as `design/screens/<NN>-<mode>-<name>.png`, phone and desktop for web: web home before the first visit tour, each tour card, home/play idle, a round in flight (up pressed), a round result, leaderboard (top of list and the own row / guest note), Coins or Missions tab, the promo video modal, profile, identity (email input), OTP dialog, share modal if the branch has it, the claim page (`/claim/<invalid>` state and, if reachable through the kiosk flow, a valid one), the update banner if it can be triggered; kiosk attract screen, each kiosk intro card, kiosk play, a kiosk win, the 5-streak QR screen (drive the streak with the fake window the kiosk spec uses), the idle reset. Skip a screen you cannot reach and say so in the report rather than faking it.
3. Sprite: `design/screens/all-screens.png`, a contact sheet of every PNG in reading order, each with its filename label under it, at least 2x pixel density, plus `design/screens/index.html` (the sheet itself) and `design/screens/README.md` listing every file with a one-line description of the state shown.
4. Dev DB: copy db/run-tests.sh with PORT=55466 and container goldrush-d5-keep; server on PORT=8807 with PUBLIC_URL=http://localhost:5369 and VITE_GAME_WS=ws://localhost:8807/ws on the Vite command line; Vite on 5369. Never set FINNHUB_TOKEN. Delete the copy and container when done. Gates: lint passes on the new script. Report to `docs/reports/d5-screen-sprite-report.md`: bad news first (screens you could not reach), the file list, the absolute path of the sprite.
