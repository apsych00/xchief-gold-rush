node_modules
dist

.vercel
.env*
!.env.example
!.env.box.example
!.env.production

# local reference images
image_*.png

# local knowledge-graph output
graphify-out

# supabase local state
supabase/.temp

# playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

# per-machine Claude Code permissions
.claude/settings.local.json

# demo kiosk launch URLs contain secrets
demo/kiosks.json

# local compose port override (IIS holds :80 on the dev machine)
docker-compose.override.yml

# self-hosted mission videos (docs/tickets/self-host-mission-videos.md): operator-swappable, same
# pattern as the ad banner images. Drop the real MP4s in ads/videos/, see ads/videos/README.md.
ads/videos/*.mp4

# plain-named env copies handed to sandboxed workers (never committed)
ENV_*.txt
design/screens/
.claude/worktrees/
docs/reports/**/*.png
docs/reports/**/*.jpg
docs/reports/**/*.jpeg
docs/reports/**/*.webp
test-results/
