# Production build of the client: the game socket is same-origin, proxied by Caddy at /ws
# (docs/box-architecture.md 1b). Vite reads this over .env for `vite build`, so a developer
# .env pointing at a local dev server can never leak into a box bundle.
VITE_GAME_WS=auto
